# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'vr-everywhere' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for vr-everywhere
  pod 'Alamofire', '~> 4.0.1'

  target 'vr-everywhereTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'vr-everywhereUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
